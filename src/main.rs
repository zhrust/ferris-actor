//use env_logger::{Builder, Env, filter::LevelFilter};
mod act;
//mod version;
//use version::get_version;

fn main() {
    //println!("Hello, world!");
    //println!("{}", get_version());
    //let env = Env::default().filter_or("MY_LOG_LEVEL", "info");
    act::show();

}
